#include <bits/stdc++.h>
using namespace std;

int main(){
    int s;
    cin >> s;
    vector<pair<int, int>> scordinates;
    for (int i=0; i<s; i++){
        int x, y;
        cin >> x;
        cin >> y;
        scordinates.push_back(make_pair(x,y));
    }
    int mines;
    cin >> mines;
    for (int)
}