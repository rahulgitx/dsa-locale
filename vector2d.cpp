#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<int>* l;
    l = new vector<int>[4];
    l[0].push_back(1);
    l[1].push_back(2);
    cout << l[0][0];

    // simpler way
    vector<int> v[];
    v[0].push_back(1);
    v[1].push_back(2);
    v[1].push_back(3);
}